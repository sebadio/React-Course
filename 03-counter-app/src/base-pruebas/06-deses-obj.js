export const usContext = (clave, nombre, edad, rango) => {
  return {
    nombreClave: clave,
    anios: edad,
    rango: rango,
    nombre: nombre,
    latlng: {
      lat: 14.1232,
      lng: -12.3232,
    },
  };
};
